namespace Cortex.Streams.Files
{
    /// <summary>
    /// Enum representing supported file formats for FileSourceOperator and FileSinkOperator.
    /// </summary>
    public enum FileFormat
    {
        CSV,
        Jsonl,
        XML
    }
}
